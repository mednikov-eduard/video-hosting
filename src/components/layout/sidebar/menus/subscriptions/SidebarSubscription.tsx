export function SidebarSubscription() {
	return (
		<nav>
			<ul>
				{/* {menu.map((menuItem, index) => {
					return (
						<MenuItem
							key={index}
							item={menuItem}
						/>
					)
				})} */}
			</ul>
		</nav>
	);
}
